import React from 'react';
import Button from 'components/Button/Button';

const Root = () => (
  <div>
    <h1>Hello World!</h1>
    <Button>Close</Button>
    <Button secondary>Remove</Button>
  </div>
);

export default Root;
